/**
 * SearchSelectField.native.tsx — stub (web-only component)
 *
 * Searchable select using DOM. On mobile, compose a SearchBar + FlatList in a BottomSheet.
 * Renders null to prevent Metro bundler failures from shared imports.
 */
import React from 'react'

export interface SearchSelectFieldProps { [key: string]: unknown }
export const SearchSelectField: React.FC<SearchSelectFieldProps> = () => null
SearchSelectField.displayName = 'SearchSelectField'
