/**
 * RichMentionInput.native.tsx — stub (web-only component)
 *
 * Uses contenteditable DOM APIs. No native equivalent.
 * Renders null to prevent Metro bundler failures from shared imports.
 */
import React from 'react'

export interface RichMentionInputProps { [key: string]: unknown }
export const RichMentionInput: React.FC<RichMentionInputProps> = () => null
RichMentionInput.displayName = 'RichMentionInput'
