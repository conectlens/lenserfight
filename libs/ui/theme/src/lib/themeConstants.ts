/** localStorage key for the user's explicit theme preference. */
export const THEME_LS_KEY = 'theme' as const

/** Cross-domain cookie name — written by themeController for unauthenticated users. */
export const THEME_COOKIE_NAME = 'lf_theme' as const
