/**
 * ScrollToTop.native.tsx — stub (web-only component)
 *
 * Scroll position is managed via ScrollView/FlatList refs on native.
 * Renders null to prevent Metro bundler failures from shared imports.
 */
import React from 'react'

export interface ScrollToTopProps {
  [key: string]: unknown
}

export const ScrollToTop: React.FC<ScrollToTopProps> = () => null
ScrollToTop.displayName = 'ScrollToTop'
