// Relocated to @lenserfight/seo (framework-free, importable by the edge Worker).
// Re-exported here so existing consumers of `@lenserfight/data/repositories`
// keep working unchanged. New code should import from '@lenserfight/seo'.
export { seoService } from '@lenserfight/seo'
export type { SEOMetadata } from '@lenserfight/seo'
