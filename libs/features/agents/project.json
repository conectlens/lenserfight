{
  "name": "agents",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/features/agents/src",
  "projectType": "library",
  "tags": ["layer:feature", "feature:agents", "scope:public", "license:oss"],
  "// targets": "to see all targets run: nx show project agents --web",
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/features/agents"
      }
    }
  }
}
