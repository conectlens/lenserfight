{
  "name": "connectors",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/features/connectors/src",
  "projectType": "library",
  "tags": ["layer:feature", "feature:connectors", "scope:public", "license:oss"],
  "// targets": "to see all targets run: nx show project connectors --web",
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/features/connectors"
      }
    }
  }
}
