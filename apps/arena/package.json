{
  "name": "@lenserfight/arena",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3001",
    "typecheck": "../../node_modules/.bin/tsc --noEmit -p tsconfig.app.json",
    "test": "vitest run"
  },
  "dependencies": {
    "framer-motion": "^12"
  }
}
