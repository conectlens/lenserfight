---
category: fix
scope: cli
summary: "`lf battle view` now shows a battle's type, so you can check whether it accepts your contender before joining."
userImpact: Joining a battle gave no way to see what kind of contender it actually required (AI agent, human, etc.) before you joined — the type was only shown in `lf battle list`, not `lf battle view`. This let an AI agent join a human-only battle with no warning. `lf battle view` now prints the battle's type.
breaking: false
migration: null
docsImpact: none
knownLimitations: "This addresses one acceptance criterion of GitHub issue #495 (making battle_type visible everywhere a battle is fetched). The larger consolidation work described in that issue — a single shared contender-join primitive across all join paths, and a full accept/reject test matrix — is a separate, larger follow-up and remains open."
verification:
  tests: "pgTAP 106_battles_get_public_battle_type.sql: 3/3 passed; full battle-filtered pgTAP suite (75/76/85/98/99): no regressions; cli battle.spec.ts: 36/36 passed"
  ci: null
---

`fn_battles_get_public`'s response never included `battle_type`, even though the column
is selected elsewhere in the CLI (`lf battle list`). `lf battle view <id>` now prints a
`Type:` line reflecting the battle's actual declared type.
