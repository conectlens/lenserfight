---
category: fix
scope: web
summary: The gateway daemons empty state now shows install/auth/start steps and a docs link instead of a bare CLI one-liner.
userImpact: A first-time visitor to /settings/gateway with no daemons registered previously saw only "Run `lf gateway serve`..." with no context on installing the CLI or authenticating first. The empty state now shows the three setup steps, a copy-to-clipboard command, and a link to the gateway CLI docs.
breaking: false
migration: null
docsImpact: none
knownLimitations: "libs/features/devices has no test runner configured (pre-existing); verified via typecheck/lint only, not automated component tests."
verification:
  tests: "tsc --noEmit clean; eslint shows only pre-existing unrelated errors"
  ci: null
---
