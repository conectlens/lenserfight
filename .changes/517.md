---
category: fix
scope: web
summary: The workflow creation wizard no longer forces a template picker, makes manual execution the clear default, and links to the workflow builder guide.
userImpact: Creating a new workflow used to open on an unskippable "pick a template" screen before you could even name your workflow, and the Schedule step only offered a recurring CRON schedule with no visible way to say "I'll just run this myself." The wizard now opens straight to naming your workflow, offers templates as an optional "Browse templates" button, and the Schedule step defaults to "Manual only (run on demand)" with CRON as an explicit opt-in. Every step also now links to the workflow builder docs.
breaking: false
migration: null
docsImpact: none
knownLimitations: null
verification:
  tests: "vitest CreateWorkflowWizard.spec.tsx: 3/3 passed; full feature-workflows suite: 326/349 passed (15 pre-existing unrelated failures tracked as #516)"
  ci: null
---
