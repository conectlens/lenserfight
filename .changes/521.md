---
category: security
scope: supabase
summary: Memory and model profiles created from inside an AI agent's workspace no longer silently vanish from list views, and creating one for an agent you don't own is now rejected.
userImpact: Creating a memory or model profile while acting inside an AI agent's own workspace succeeded, but the profile then disappeared from the list immediately after — it was actually saved, just invisible. Fixed by aligning the list check with the same ownership rule the create path already uses. Separately, a caller could previously create these profiles for any agent ID at all, not just ones they own; that gap is now closed.
breaking: false
migration: null
docsImpact: none
knownLimitations: "Other fn_create_workspace_record allowlist tables (workflow_assignments, evaluation_*, tools_registry, team_members, team_edges, agent_run_events, agent_run_steps) don't key off ai_lenser_id and are not covered by this hardening — a separate audit."
verification:
  tests: "pgTAP 108_memory_profiles_ownership.sql: 4/4 passed; 93_workspace_rpcs_repairs.sql: 8/8 passed; full agent-filtered pgTAP suite: 26/26 passed"
  ci: null
---
