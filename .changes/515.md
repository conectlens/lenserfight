---
category: fix
scope: api
summary: Building a workflow with a mistyped node type or missing required config now fails immediately with a clear error, instead of silently entering the graph and failing later at execution time.
userImpact: Creating or validating a workflow through the MCP tools (`create_workflow`, `validate_workflow`) previously accepted any node type string and any config shape with no checks — a typo'd node type or a missing required field (like an `http_request` node with no URL) would pass validation and only surface as a confusing runtime failure once the workflow actually ran. Both tools now check every node's config against the workflow node catalog and report the missing field or unknown type up front.
breaking: false
migration: null
docsImpact: updated
knownLimitations: "Only checks presence of required config fields (per the catalog's requiredConfig), not value shape/type. A separate pre-existing, unrelated test failure in the workflow document-import feature (schedule activation) was found during this work and is tracked separately."
verification:
  tests: "nx test infra-execution: 876/876 passed; mcp-server jest suite: 270/270 passed; typecheck and eslint clean on all changed files"
  ci: null
---

Fixing this also surfaced that the catalog's `schedule_trigger` node declared its required
config field as `cron`, while the actual runner, the MCP tool, and the web config form all
already used `cronExpression`. The catalog now matches the three implementations that were
correct.
