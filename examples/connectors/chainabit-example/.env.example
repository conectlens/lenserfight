# Copy to .env and fill in.
# The service token is printed by:
#   lenserfight connectors add chainabit --scopes lenses:read
LENSERFIGHT_SERVICE_TOKEN=lf_replace_me

# Webhook target that the connector dispatches events to
LENSERFIGHT_CONNECTOR_ENDPOINT=https://example.invalid/webhook
